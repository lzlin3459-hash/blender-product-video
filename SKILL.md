---
name: blender-product-video
description: "用 Blender 将产品参考图或现有三维模型制作成可编辑产品资产、展示图和带旁白的电商展示视频。适用于包装桶、瓶罐、盒装产品等外观展示，以及同一资产的后续修改；不用于校园或建筑建模、纯方案讨论、指令文件审查、纯图片生成、单纯视频剪辑、单独配音或工程级尺寸复刻。"
metadata:
  instruction-revision: "2026-09-17"
---

# Blender 产品展示制作

交付真实可编辑的 Blender 工程和从场景渲染的媒体。根据用户当前阶段推进，不把完整流程强加给只要求建模、配音或局部修改的任务。

## 开始与输入

- 首次接手或工程版本变化时，只读取当前步骤所需的工程和版本说明；上下文中已有的验收结论直接复用；已确认的模型、旁白和镜头继续沿用，不重复索要确认。
- 新产品只补问会影响结果的信息：使用场景/画幅、参考图或模型、尺寸、必须保持的结构与标签、是否需要声音。可合理估算的内容明确标注后先做样板。
- 只有效果图也可开始外观样板；不能据此承诺完整标签、真实尺寸、隐藏结构或二维码可扫描性。
- 首次执行或环境变化时检查 Blender 可执行文件、磁盘与可用硬件；复用仍有效的检查结果。大型渲染前核实剩余磁盘空间。优先使用现有本地安装和 bpy；MCP、Computer Use 不是前置条件。需要安装时核实官方来源和校验值，不能把社区 Blender MCP 称为官方插件。

## 范围与收尾

- 开始制作视频时，用简短说明固定用途、时长、画幅、镜头和交付物；只做局部修改时沿用已有范围。不把“更好看”扩展为全资产重建。
- 尽早做覆盖全部镜头的低清片；需要声音时加入临时或已有音轨、字幕，先检查整体节奏与信息可读性，再精修。
- 精度按正式镜头的可见程度分配。整体轮廓、品牌与标签优先于看不到的结构；资料缺失时标注限制，必要时调整展示角度，不凭空补齐品牌文字。
- 小项目可建议一轮整体修改加一轮收尾，轮次不是拒绝用户新需求的硬限制。达到约定标准就交付，不主动生成无尽的下一轮细节任务；新要求先说明对范围与成本的影响。
- 保留原素材和已通过版本。既有授权覆盖常规本地修改；新增付费、未授权发布、不可逆删除或覆盖用户成果，在具体操作前确认。

## 按成果推进

1. **可编辑模型与一张外观样板。** 有模型先检查单位、结构、材质与转换结果；没有模型则从可解释的几何体开始。保留参考文件。标签中的品牌、文字和图案尽量直接使用用户原始素材，不靠生成式改写补全模糊内容。
2. **覆盖实际需要的视角。** 使用多张效果图重建标签时，先读 [标签重建](references/label-reconstruction.md)。正面投影只说明正面可用，不能作为完成360度资产的依据。需要完整环绕时检查四周及拼接边界；仅做固定视角时检查实际可见区域，并说明未覆盖方向。
3. **低清运镜预览。** 先定用途和画幅，检查关键帧再渲染序列。镜头服务于产品识别和重点展示；近景允许有意裁切，整体镜头检查提手/顶盖/底部是否出画。保留已通过版本，在新版本继续。
4. **按需配音。** 若请求旁白，参考 [动画、声音与交付](references/motion-and-delivery.md)。沿用用户提供或已确认的文案；用户已授权起草并生成时，自查文案后继续，无需另设确认关卡；只从可确认资料提取信息，包装标语不能自动升级为性能证明。音色、语速和运镜已有确认时直接沿用。
5. **高清输出。** 从三维工程原生重渲染，不能把低清放大称为高清重渲染。复用相同场景与目标规格下有效的单帧耗时；缺少有效数据时再测量并估算整段；长时间本机渲染持续完成，并保留帧序列以便恢复。
6. **验证并交付。** 仅检查本次实际交付物：模型检查结构、材质与依赖；视频按 [动画、声音与交付](references/motion-and-delivery.md) 的完成标准检查。该参考为视频交付检查的唯一清单，不在多个阶段重复执行同一版本的检查。给出实际交付文件的绝对路径链接，并说明残留限制。

样板和预览是减少返工的检查点，不是每阶段都必须询问用户的许可关卡。用户要求整段完成且必要输入充分时，先自行检查并继续；只有必须由用户决定的内容才提问。发布到店铺不属于本地制作的默认范围。

## 可复用资源

- [标签重建](references/label-reconstruction.md)：缺少平面展开稿、多视图投影、接缝和原图光影问题。
- [动画、声音与交付](references/motion-and-delivery.md)：镜头、帧数、旁白时长、编码与完成标准。
- [项目经验与示例](references/verified-case.md)：仅在复用该案例或示例时读取，不作为其他项目的必读资料。
- `scripts/render_frames.py`：从已有打包场景输出可恢复的PNG帧序列，见下例。它不创建模型、不配置动画、不自动生成旁白。

```powershell
& '<blender.exe>' --background --python '<skill目录>/scripts/render_frames.py' -- --scene '<已打包工程.blend>' --output '<新渲染目录>' --width 1080 --height 1080 --samples 48 --threads 8
```

脚本用工程哈希、Blender版本和渲染参数核对续跑条件；通过临时PNG完成后重命名，避免把中断帧误认成已完成。修改工程或参数后使用新目录。需要更换引擎、设备或相机时先修改并保存输入工程。
